/*
function init(){
}

if (document.readyState === "loading") {//might be 'complete' or 'loading'
    document.addEventListener("DOMContentLoaded", init);
} else {//dom already loaded, so the above event will never fire, so:  
    init();
}
*/